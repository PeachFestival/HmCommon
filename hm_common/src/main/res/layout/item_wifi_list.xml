<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/hm_item_wifi_bg"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingVertical="16dp"
    android:paddingHorizontal="14dp">

    <!-- 信号图标与锁图标容器 -->
    <RelativeLayout
        android:layout_width="34dp"
        android:layout_height="34dp"
        android:layout_marginEnd="16dp">

        <ImageView
            android:id="@+id/iv_wifi_signal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/hm_ic_wifi_4" />

        <ImageView
            android:id="@+id/iv_wifi_lock"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:src="@drawable/hm_ic_wifi_lock"
            android:visibility="gone"
            tools:visibility="visible" />
    </RelativeLayout>

    <!-- WiFi 名称与状态 -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_wifi_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="#222222"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="TP-LINK_5G" />

        <TextView
            android:id="@+id/tv_wifi_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="#777777"
            android:textSize="14sp"
            tools:text="已连接 (192.168.1.105)" />
    </LinearLayout>

    <!-- 已连接指示图标 -->
    <ImageView
        android:id="@+id/iv_wifi_connected"
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:layout_marginStart="12dp"
        android:src="@drawable/hm_ic_wifi_check"
        android:visibility="gone"
        tools:visibility="visible" />

</LinearLayout>
